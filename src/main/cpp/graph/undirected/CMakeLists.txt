add_library(depth-first-search OBJECT
            depth-first-search.h)

add_library(depth-first-paths OBJECT
            depth-first-paths.h)

add_library(breadth-first-paths OBJECT
            breadth-first-paths.h)
